# Configuración para desarrollo - Sin autenticación
# Para habilitar autenticación en dev, cambia el profile activo a 'auth'
spring.profiles.active=default

# JWT Configuration para desarrollo (opcional, solo si usas profile 'auth')
# spring.security.oauth2.resourceserver.jwt.issuer-uri=https://dev-domain.auth0.com/
# spring.security.oauth2.resourceserver.jwt.audience=dev-api-identifier

# Logs más verbosos en desarrollo
logging.level.com.notivest.pricefetcher.security=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.reactive=DEBUG
